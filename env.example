# Flask Configuration
FLASK_ENV=production
FLASK_APP=da.app

# Secret Key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your-secret-key-here-change-in-production

# Database URL
# For SQLite (default):
DATABASE_URL=sqlite:///instance/devices.db
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/device_accounting
# For MySQL:
# DATABASE_URL=mysql://user:password@localhost:3306/device_accounting

# Application Port
APP_PORT=5001

# Email/SMTP Settings (для уведомлений об удалении)
# Письма отправляются с da@ittest-team.ru на email супер-админа
SMTP_HOST=mail.ittest-team.ru
SMTP_PORT=465
SMTP_USER=da@ittest-team.ru
SMTP_PASSWORD=R8mq9uZ6`u


