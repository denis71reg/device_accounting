# IT Test Buddy - Система онбординга и учета

Корпоративная система онбординга и учета сотрудников IT Test, состоящая из:
- **Web-Админки (Django Admin)** - для HR и администраторов
- **Telegram Бота (Aiogram)** - для сотрудников и кандидатов

## Стек

- Python 3.11+
- Django 5.x
- Aiogram 3.x
- PostgreSQL
- Redis + Celery (для отложенных задач)
- Docker (опционально)

## Быстрый старт

### 1. Установка зависимостей

```bash
cd ittest_buddy
python -m venv venv
source venv/bin/activate  # на Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Настройка базы данных

Создайте PostgreSQL базу данных:

```sql
CREATE DATABASE ittest_buddy;
CREATE USER your_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE ittest_buddy TO your_user;
```

### 3. Настройка переменных окружения

```bash
cp .env.example .env
# Отредактируйте .env и укажите:
# - SECRET_KEY (сгенерируйте через: python -c "import secrets; print(secrets.token_hex(32))")
# - Параметры БД
# - TELEGRAM_BOT_TOKEN (получите у @BotFather)
# - TELEGRAM_HR_CHAT_ID (ID чата для получения документов)
```

### 4. Применение миграций

```bash
python manage.py migrate
```

### 5. Создание суперпользователя

```bash
python manage.py createsuperuser
```

### 6. Заполнение начальных данных (BotMessage)

После создания суперпользователя зайдите в админку и создайте необходимые сообщения бота:

- `welcome_msg` - Приветствие новичка
- `docs_request` - Список необходимых документов
- `vpn_instruction` - Инструкция по VPN
- `email_instruction` - Инструкция по почте/подписи
- `lunch_tula_tomorrow` - Сообщение про обед (Тула, до 12:00)
- `lunch_tula_closed` - Сообщение про обед (Тула, после 12:00)
- `lunch_spb_today` - Сообщение про обед (СПб, до 14:30)
- `lunch_spb_closed` - Сообщение про обед (СПб, после 14:30)
- `return_device_text` - Текст про сдачу оборудования
- `info_contacts` - Информация и контакты

### 7. Запуск сервера Django

```bash
python manage.py runserver
```

Админка будет доступна по адресу: http://localhost:8000/admin/

### 8. Запуск Telegram бота

В отдельном терминале:

```bash
python manage.py run_bot
```

### 9. Запуск Celery Worker (для отложенных задач)

В отдельном терминале:

```bash
celery -A ittest_buddy worker --loglevel=info
```

### 10. Запуск Celery Beat (для периодических задач)

В отдельном терминале:

```bash
celery -A ittest_buddy beat --loglevel=info
```

## Использование

### Работа HR в админке

1. **Создание сотрудника:**
   - Зайдите в раздел "Пользователи"
   - Нажмите "Добавить пользователя"
   - Заполните обязательные поля: Username, ФИО, Start Date, Location
   - Статус по умолчанию: `pre_hire`

2. **Смена аккаунта (Re-invite):**
   - Выберите сотрудника
   - В действиях выберите "Сгенерировать ссылку перепривязки"
   - Скопируйте ссылку из поля "Ссылка перепривязки" и отправьте сотруднику

3. **Назначение оборудования:**
   - Зайдите в раздел "Устройства"
   - Выберите устройство и укажите владельца
   - Укажите дату выдачи

4. **Рассылки:**
   - В разделе "Пользователи" выберите нужных сотрудников галочками
   - В действиях выберите "Отправить сообщение выбранным"
   - Введите текст сообщения

5. **Редактирование сообщений бота:**
   - Зайдите в раздел "Сообщения бота"
   - Выберите нужное сообщение и отредактируйте текст

### Работа сотрудника в боте

1. **Первый вход:**
   - HR создает карточку сотрудника в админке
   - Сотрудник запускает бота через `/start`
   - Если статус `pre_hire` - начинается сбор документов

2. **Сбор документов:**
   - Бот запрашивает необходимые документы
   - Сотрудник отправляет фото/документы
   - После отправки всех документов нажимает "✅ Я всё отправил"
   - Статус меняется на `ready_to_start`

3. **День 1:**
   - Утром (09:00) бот автоматически отправляет инструкции
   - Меняется статус на `active`
   - Отправляются инструкции: VPN, почта, обед (если применимо)

4. **Работа:**
   - Главное меню с кнопками: "📱 Мое оборудование", "ℹ️ Инфо / Контакты"

## Структура проекта

```
ittest_buddy/
├── ittest_buddy/          # Основные настройки Django
│   ├── settings.py        # Конфигурация
│   ├── urls.py           # URL-маршруты
│   ├── celery.py         # Настройка Celery
│   └── celerybeat_schedule.py  # Расписание задач
├── core/                  # Основное приложение
│   ├── models.py         # Модели: User, Device, BotMessage
│   ├── admin.py          # Настройка админки
│   └── views.py          # Представления (рассылки)
├── bot/                   # Telegram бот
│   ├── bot.py            # Основная логика бота
│   ├── tasks.py          # Celery задачи
│   └── management/commands/run_bot.py  # Команда запуска
├── templates/             # Шаблоны
├── manage.py             # Точка входа Django
└── requirements.txt      # Зависимости
```

## Модели данных

### User (Пользователь)
- `telegram_id` - ID в Telegram (уникальный, может быть null)
- `username` - Никнейм (@username)
- `full_name` - ФИО
- `status` - Статус: pre_hire, ready_to_start, active, alumni
- `start_date` - Дата выхода на работу
- `location` - Офис: tula, spb, remote
- `role` - Должность (Dev, QA, PM)
- `reinvite_token` - UUID для перепривязки аккаунта

### Device (Устройство)
- `name` - Название устройства
- `inventory_number` - Инвентарный номер
- `holder` - Владелец (FK -> User)
- `issued_at` - Дата выдачи

### BotMessage (Сообщение бота)
- `slug` - Технический код (уникальный)
- `text` - Текст сообщения (HTML/Markdown)
- `description` - Описание для админа

## Автоматические задачи

- **Ежедневно в 09:00** - Отправка инструкций "День 1" пользователям с `start_date = сегодня` и статусом `ready_to_start`

## Безопасность

- Бот не сохраняет файлы документов на диск сервера
- Только транзитная пересылка в защищенный чат HR
- Все токены и пароли хранятся в переменных окружения

## Развертывание

Для продакшена рекомендуется использовать:
- Gunicorn + Nginx для Django
- Supervisor или systemd для управления процессами (бот, celery worker, celery beat)
- PostgreSQL для БД
- Redis для Celery

## Поддержка

При возникновении проблем обращайтесь к разработчикам IT Test.




